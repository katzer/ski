compile: &defaults
  build: .
  volumes:
    - .:/code:rw
  command: rake compile
bintest:
  <<: *defaults
  command: /bin/sh -lc "rake test:bintest"
clean:
  <<: *defaults
  command: rake clean
release:
  <<: *defaults
  command: rake release
shell:
  <<: *defaults
  command: sh -l
